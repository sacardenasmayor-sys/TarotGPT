{
    "name": "@tarot-gpt/deck-engine",
    "version": "0.0.0",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "lint": "eslint .",
        "test": "vitest run --reporter=verbose --reporter=junit --outputFile=test-report.xml",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^4.0.16",
        "fast-check": "^4.5.3",
        "typescript": "^5.0.0",
        "vitest": "^4.0.16"
    }
}